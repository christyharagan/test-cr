import { useState } from 'react'
import Banner from '@ddsweb/banner';

const SimpleBanner2 = (props) => {
    // return <Banner headline={props.title} image={{ alt: 'yarg' || ' ', large: props.background.src }} />
	// return <Banner
	// 	headline={props.title}
	// 	image={{ alt: 'yarg' || ' ', large: props.background.src,}}
	// 	/>
};